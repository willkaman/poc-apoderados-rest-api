spring:
  application:
    name: ${project.artifactId}
    version: ${project.version}
    namespace: ${banchile.system.namespace}

  cloud:
    config:
      username: ${banchile.config.client.user} 
      password: ${banchile.config.client.pass}
      
eureka:
  client:
    register-with-eureka: true
    prefer-same-zone-eureka: true
    use-dns-for-fetching-service-urls: false

---
spring:
  profiles: localdev
  cloud:
    config:
      uri: http://infra.banchile.box:8890
  
eureka:
  client:
    serviceUrl:
      defaultZone: ${discover.server.url.localdev}

banchile:
  config:
    client:
      user: localdev
      pass: Nicholson4567

---
spring:
  profiles: desarrollo
  cloud:
    config:
      uri: http://config.bantcent.cl:8890
  
eureka:
  client:
    serviceUrl:
      defaultZone: ${discover.server.url.desarrollo}
    
banchile:
  config:
    client:
      user: desarrollo
      pass: Nicholson4567

---
spring:
  profiles: laboratorio
  cloud:
    config:
      uri: http://config.bantcent.cl:8890
  
eureka:
  client:
    serviceUrl:
      defaultZone: ${discover.server.url.laboratorio}

---
spring:
  profiles: produccion
  cloud:
    config:
      uri: http://config.bantcent.cl:8890
  
eureka:
  client:
    serviceUrl:
      defaultZone: ${discover.server.url.produccion}
